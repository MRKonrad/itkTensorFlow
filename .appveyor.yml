#-------------------#
#   itkTensorFlow   #
#-------------------#

os:
  - Visual Studio 2017

init:
  - git config --global core.autocrlf input
  # Set build version to git commit-hash
  - ps: Update-AppveyorBuild -Version "$($env:APPVEYOR_REPO_BRANCH) - $($env:APPVEYOR_REPO_COMMIT)"

environment:
  ZIP_EXTENDED_NAME: itkTensorFlow_%APPVEYOR_REPO_TAG_NAME%_windows_%PLATFORM%

install:
  - git submodule update --init --recursive

platform:
  - Win32
  - x64

configuration:
  - Release

before_build:
  - echo %PLATFORM%
  - echo %configuration%
  - dir
  - cd
  - cd ..
  - cd
  - curl -L -O https://www.dropbox.com/s/fu5we0o2j1a4uzt/ITK412_win_x64_install.zip
  - dir
  - cd
  - 7z x -r ITK412_win_x64_install.zip
  - dir
  - cd itkTensorFlow
  - cd
  - dir
  - if [%PLATFORM%]==[Win32] (
        cmake . -Bbin -DCMAKE_INSTALL_PREFIX=install -DITK_DIR_HINTS="../ITK_install" -G"Visual Studio 15 2017" -DCMAKE_BUILD_TYPE=%configuration%
      )
  - if [%PLATFORM%]==[x64] (
        cmake . -Bbin -DCMAKE_INSTALL_PREFIX=install -DITK_DIR_HINTS="../ITK_install" -G"Visual Studio 15 2017 Win64" -DCMAKE_BUILD_TYPE=%configuration%
      )
  - cd
  - dir bin

build:
  project: bin\itkTensorFlow.sln  # path to Visual Studio solution or project
  parallel: true           # enable MSBuild parallel builds
  verbosity: normal        # MSBuild verbosity level {quiet|minimal|normal|detailed}

after_build:
  # install the files
  - cd bin
  - dir
  - cmake --build . --config %CONFIGURATION% --target BUILD_ALL
  - cmake --build . --config %CONFIGURATION% --target INSTALL
  - dir %CONFIGURATION%
  - cd ..
  - dir install
  - cd install
  - 7z a %ZIP_EXTENDED_NAME%.zip * -r # zip
  - cd ..
  - mkdir deployment
  - copy install\%ZIP_EXTENDED_NAME%.zip deployment\%ZIP_EXTENDED_NAME%.zip
  - dir
  - dir deployment

test_script:
  # TODO: should I add sth like this? https://github.com/mgerhardy/caveexpress/blob/master/appveyor.yml
  - cd
  - dir
  - cd bin\tests\%CONFIGURATION%
  - itkTensorFlowTests.exe
  - cd ..\..\..

artifacts:
  - path: deployment\*.*

deploy:
  provider: GitHub
  auth_token:
    secure: Nmbbo4JCoK5AuN1ugWmE/c8/BAXlpz8Lb7piWDcAAWnT/GFa7fwL20GbXyU9AZSn
  draft: false
  prerelease: false
  force_update: true
  on:
    appveyor_repo_tag: true        # deploy on tag push only
    configuration: Release
